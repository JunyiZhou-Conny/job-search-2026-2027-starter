# Baseten — Software Engineer - Dedicated Inference

| Field | Value |
|---|---|
| Date | 2026-08-23 |
| Ledger id | none |
| ATS | Ashby |
| Apply URL | https://jobs.ashbyhq.com/baseten/fc6e5f2e-eb2d-4a6c-8a51-8422e8662bde |
| Compensation on page | **$165,000 – $330,000** / year |
| `form_status` | `drafted` |
| `submitted` | no |
| Identity Copilot filled | name, email, phone, LinkedIn, resume |
| Copilot need-review | 2 (why Baseten; developer-facing APIs/SDKs/CLI) |

**Not submitted.** EEO filled → block unattended Submit.
If a salary field appears, type `165000` (page minimum).

---

## What about Baseten and this role interests you?

Do not invent inference-platform production on-call experience.

**Draft:**

```text
I want to work on the serving path that other people's models actually hit. Dedicated inference is that job: latency, isolation, and an API someone else can trust. I am not claiming I have run a multi-tenant GPU inference fleet.

I am a Harvard SM student in Health Data Science (program end 2026-12-18; commencement March 2027). The closest work I have is scientific software that has to keep running. On FASRC Cannon I built a closed-loop experiment runner (338 counted runs, 79.3 hours of wall-clock compute) with checkpoint/resume and a hard cost ceiling on an optional LLM planner that has never been used on a real run — the live planner is the rule reasoner. At Emory Pediatric Hospital I shipped a Flask + React airway-management chatbot with Auth0, MongoDB Atlas Vector Search, Elastic Beanstalk, and CloudFront so a clinician demo stayed up.

I like handing watch-and-retry to automation. I like asking questions in the same room. I will relocate.
```

---

## Have you built developer-facing products such as APIs, SDKs, or CLI tools?

Yes, with the exact surfaces in the evidence bank. Do not add an SDK
that is not there.

**Draft:**

```text
Yes — APIs and a CLI, not a public SDK.

The airway-management chatbot is a React client on Flask REST endpoints, with Auth0 in front and MongoDB Atlas Vector Search on the retrieval path. The AlphaFold protein–nucleic acid pipeline at Emory Biochemistry included a CLI so non-ML users could run GPU inference and apply pLDDT/PAE filters without sitting in the notebook. I have not shipped a versioned third-party SDK.
```
