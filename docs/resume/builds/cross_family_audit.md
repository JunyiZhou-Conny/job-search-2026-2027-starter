# Cross-family audit. Resume Stack v1

Four frozen one-pagers. Same candidate. Four identities.

Heuristic scores were not used.

## Identities in 15 seconds

- `swe_v1`. Owned software. Compile and tests. A public API. An ETL pipeline with AWS.
- `ml_ai_v1`. Model comparison. LoRA ablation. External-leaderboard generalization.
- `ai_infra_v1`. Experiment loop. Gated browser automation. Hub and honest eval.
- `health_ai_v1`. Clinical simulation product. Mouse-to-human translation. HVG method port.

A recruiter can tell why each PDF exists.

## Project map

| project | swe | ml_ai | ai_infra | health_ai |
|---|---|---|---|---|
| job_search_os | compile, tests, recovery | | agent gates, action sheets | |
| mixhvg_py | AnnData API, pytest | | | HVG method, numerical correction |
| compleg_uk_nz | ETL, schema, AWS | | | |
| cellot_wyss | | OT vs scGen, split, gene-space flip | hub, 43 GB I/O, decoded eval | translation, biological reading of the flip |
| vlm_textvqa_lora | | PEFT and prompt ablation | | |
| cv_pneumonia | | Kaggle generalization | | |
| autoresearch_cellot | | | closed-loop search | |
| airway_chatbot | | | | clinical product |

Repeats are not cosmetic. Job Search OS and speciesOT change jobs by family. mixhvg changes jobs by family.

## Lead signals

- SWE. First heading is an owned system with secret-refusing tests.
- ML/AI. First heading is a named model comparison with a split that fails.
- AI Infra. First heading is a closed-loop cluster system with a job count.
- Health AI. First heading is a HIPAA-conscious clinical platform.

## Strength and crowding

ML/AI is the densest page. Two labeled course projects sit under a lab comparison. That is honest. The visual pass dropped the DenseNet / ResNet percents to open the bottom margin.

SWE and Health AI have more whitespace. Compleg is the oldest project and the only AWS EC2/S3 hands-on line. Keep it.

AI Infra stays the systems page. Do not pull AutoResearch onto SWE or ML/AI to make the four pages symmetrical.

## Concerns for a human before activation

1. Registry rows are inactive. Polar still prefers Simplify and will mark an empty widget `missing_production_resume`.
2. This VM has `SIMPLIFY_EMAIL=[REDACTED]`. A real apply export needs `RESUME_EMAIL` or a usable `SIMPLIFY_EMAIL`.
3. speciesOT $R^2$ is a paired held-out-species prior, not Tabula atlas OOD. Interviewers will ask. Answers live in each family's `interview_defense.md`.
4. Airway master verbs Architected and Led were not copied. Confirm that Scrum Master plus team of 6 is the intended public story.
5. Two course projects on `ml_ai_v1` are labeled. Do not later strip the course labels to make the page look like internships.
6. Do not activate until ROUTE exists or an owner picks a default attach by hand.
