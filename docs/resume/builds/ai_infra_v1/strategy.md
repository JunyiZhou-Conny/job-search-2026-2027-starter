# Narrative and strategy. `ai_infra_v1`

## Internal thesis

ML and AI engineer who builds experimentation, agent, and research infrastructure.

Do not print that sentence on the resume.

## Section order

Engineering Projects, then Research Experience. Matches `knowledge/resume_philosophy.yaml` `ai_infra.section_titles`.

Engineering. AutoResearch, Job Search OS, mixhvg-py.
Research. speciesOT.

## Per project

### autoresearch_cellot

Role. Lead engineering story. The experiment loop.

Dimensions. Loop. Proposer and agenda checkpoint. Preemption and fairshare-as-design. Optional LLM with fallback, never exercised.

Omit. 12 tiers. 7 gene-space strings. 21 proposals.jsonl. Live LLM episode. Cost reduced 200-500x. Calling it an agent. Catalog wording that says "fairshare-aware scheduler". Bank says `sshare fairshare_floor` is a STOP reason, not a throttle.

Verbs. Sole author. Built. Split. Implemented. Routed as design, not as measured savings.

Follow-ups. See `interview_defense.md`.

### job_search_os

Role. Agent-systems story with human gates.

Dimensions. Plane split. Compiled runtime. HITL Submit. Hands-only Computer Use. Failure recovery.

Omit. Fully autonomous apply. Submitted-application counts. Live unittest totals. Polar uploads of `JZ_resume.pdf`. Cloud as the production operator.

Verbs. Sole author. Built. Compiled. Encoded. Do not write architected.

### mixhvg_py

Role. Short scientific-software proof. Validation infra.

Dimensions. AnnData API and max-rank ensemble. Measured fidelity and the median-vs-mean correction.

Omit. mv_ct / mv_nc match. Bit-exact scran_pos. Production use outside speciesOT. "Translated R to Python" as the whole story.

Verbs. Sole author. Ported. Validated. Found. Do not write designed the method. Zhao et al. own the method.

### cellot_wyss

Role. Research-infra story. Hub and honest eval.

Dimensions. No-auto-submit hub. Atlas-scale I/O. Rat-in-train R^2 ranges with the gene-space flip.

Omit. Drug-effect composition. DatasetHandle / FrozenAE / JobPlan on main. 180+ models. New unpublished scorecard decimals. Unbalanced OT as the headline. TF-to-PyTorch 2018 port. Three separate CellOT projects.

Verbs. Graduate Researcher. Implemented. Built. Engineered. Bank uses those.

## Skills line

Keep languages and compute the selected projects use. Python. PyTorch. SLURM. YAML. AnnData. unittest. Polar compiler.

Drop Kubernetes. Drop Spark/Hadoop/Snowflake stuffing. Drop JavaScript and React. No selected project uses them on this page.

AWS certifications stay as profile facts already on the master.

## Catalog

Do not use a stronger catalog paraphrase. Bank sentences only.
