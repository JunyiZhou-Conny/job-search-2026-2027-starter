# Narrative and strategy. `ai_infra_v1`

## Internal thesis

ML and AI engineer who builds experimentation, agent, and research infrastructure.

Do not print that sentence on the resume.

## Section order

Engineering Projects, then Research Experience. Matches `knowledge/resume_philosophy.yaml` `ai_infra.section_titles`.

Engineering. AutoResearch, human-supervised browser automation.
Research. speciesOT.

## Recruiter-facing rules for this cleanup

Provenance limits stay in the bank and in `interview_defense.md`. Do not spend resume words on what was not done when the true claim can stand.

Translate repo-internal names into engineering language. Keep technical depth.

## Per project

### autoresearch_cellot

Role. Lead engineering story. The experiment loop.

Dimensions. Loop and measured Cannon volume. Deterministic planner plus checkpointed agenda. Optional LLM planner infrastructure. Preemption and fairshare-as-design.

Omit on the page. 12 tiers. 7 gene-space strings. 21 proposals.jsonl. Live LLM episode. Cost reduced 200-500x. Calling it an agent. Catalog wording that says "fairshare-aware scheduler". Bank says `sshare fairshare_floor` is a STOP reason, not a throttle. Function names `policy.validate()`, `serial_requeue`, `cache/last.pt`. The sentence "has not been used on recorded runs".

On-page wording. Deterministic one-delta planner, gated by a validation policy, checkpointed agenda, optional LLM planner infrastructure with token and cost accounting and a spend ceiling that falls back to the rule planner. Resume preempted jobs from the last checkpoint.

Verbs. Sole author. Built. Split. Routed as design, not as measured savings.

Follow-ups. See `interview_defense.md`. The unused-LLM boundary lives there.

### job_search_os

Role. Agent-systems story with human gates.

Recruiter-facing title. `Human-Supervised Browser Automation System`. Bank title stays Job Search OS. Do not invent a different domain. First bullet names job applications.

Dimensions. Plane split. Compiled runtime. HITL Submit. Compiled action sheets. Failure recovery.

Omit on the page. Fully autonomous apply. Submitted-application counts. Live unittest totals. Polar uploads of `JZ_resume.pdf`. Cloud as the production operator. G2 / G3. `policy.validate`. Polar runtime compiler. The sentence "The system does not send messages or apply on its own".

On-page wording. Local browser operator reads one compiled runtime. Submit is allowed only after listed identity and writing checks. Browser steps follow a compiled action sheet. Designed recovery for Mac shutdown, Wi-Fi loss, and browser restart.

Verbs. Sole author. Built. Compiles. Designed recovery. Do not write architected.

### cellot_wyss

Role. Research-infra story. Hub and honest eval.

Dimensions. No-auto-submit hub. Atlas-scale I/O. Paired held-out-species R^2 ranges with the gene-space flip.

Omit on the page. Drug-effect composition. DatasetHandle / FrozenAE / JobPlan on main. 180+ models. New unpublished scorecard decimals. Unbalanced OT as the headline. TF-to-PyTorch 2018 port. Three separate CellOT projects. The parenthetical "LPS / rat-in-train prior, not the Tabula atlas".

On-page wording. Paired held-out-species evaluation. Decoded-space gap-closed rather than raw R^2. Provenance of the split stays in `interview_defense.md`.

Verbs. Graduate Researcher. Implemented. Built. Engineered. Bank uses those.

### mixhvg_py

Not on this page. See `selection.md`. Framing notes kept only so a later family can reuse them.

Role. Short scientific-software proof. Validation infra.

Dimensions. AnnData API and max-rank ensemble. Measured fidelity and the median-vs-mean correction.

Omit. mv_ct / mv_nc match. Bit-exact scran_pos. Production use outside speciesOT. "Translated R to Python" as the whole story.

Verbs. Sole author. Ported. Validated. Found. Do not write designed the method. Zhao et al. own the method.

## Skills line

Recognizable technologies the selected projects use. Python. C++. SQL. R. PyTorch. TensorFlow. scanpy. AnnData. YAML. SLURM. FASRC Cannon. job arrays. checkpoint/resume. unittest. pytest.

Drop from the skills line. Kubernetes. Spark/Hadoop/Snowflake stuffing. JavaScript and React. Polar runtime compiler. `policy.validate`. decoded-frame metrics. Computer Use. G2 / G3.

AWS certifications stay as profile facts already on the master.

## Catalog

Do not use a stronger catalog paraphrase. Bank sentences only.
