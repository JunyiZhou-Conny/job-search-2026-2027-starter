# Rejected evidence. `ai_infra_v1`

See `selection.md` for the full reject list.

Resume-facing omissions inside selected projects.

## autoresearch_cellot

- 12 tiers / 7 gene-space strings. README states them. Public CSV is empty. `do_not_claim`.
- 21 proposals.jsonl and one rule episode. Cannon-only. `resume_ok: false`.
- LLM directed experiments. Forbidden. Optional LLM infrastructure may appear. The unused-on-recorded-runs fact stays here.
- Cost reduced 200-500x. Forbidden.
- Agent / agentic title. Forbidden by `control_logic`.
- `policy.validate()`, `serial_requeue`, `cache/last.pt` as recruiter-facing names.

## job_search_os

- 228 tests / 21 files. Rots. `resume_ok: false`.
- 20 applied rows. Not a submit claim. `resume_ok: false`.
- Fully autonomous apply. `do_not_claim`.
- Polar uploads the two-page master. `do_not_claim`.
- G2 / G3, Polar runtime compiler, and "does not send messages or apply on its own" as resume prose. Gates stay in `interview_defense.md`.

## cellot_wyss

- Drug-effect composition. `not_yet_built`.
- DatasetHandle / FrozenAE / JobPlan / ReferenceBundle on main. `not_yet_built`.
- 180+ models and 184 experiments. `do_not_claim`.
- 2018 TensorFlow to PyTorch port. Current bank no longer measures it.
- Unbalanced OT as the headline. Side path only.
- "LPS / rat-in-train prior, not the Tabula atlas" as a resume parenthetical. Split provenance stays here.

## mixhvg_py

Dropped from this page. See `selection.md`.

- mv_ct / mv_nc agreement. `do_not_claim`.
- Production use outside speciesOT. `do_not_claim`.
- Line-by-line translation as the story. `do_not_claim`.
