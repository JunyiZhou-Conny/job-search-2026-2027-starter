# Narrative and strategy. `swe_v1`

## Internal thesis

Software engineer who owns systems other people can run. Interfaces. Schemas. Tests. Recovery.

Do not print that sentence on the resume.

## Section order

Engineering Projects, then Research Experience. Matches `knowledge/resume_philosophy.yaml` `swe.section_titles`.

Engineering. Job Search OS, mixhvg-py.
Research. Compleg.

## Recruiter-facing rules

Provenance limits stay in the bank and in `interview_defense.md`.

Translate repo-internal names into engineering language. Job Search OS stays a job-application system. mixhvg-py leads with API and tests, not biology.

## Per project

### job_search_os

Role. Lead engineering story. Owned production-shaped software.

Unique signal. Policy compile, Submit gates, secret-refusing tests, recovery.

Page space. Plane split. Compiled runtime. Tests that refuse leaked secrets. Designed recovery.

Deliberately omitted. Autonomous apply. Applied-row counts. Live unittest totals. Polar compiler. G2 and G3. Cloud as the production operator. Calling it an agent system. That wording belongs on `ai_infra_v1`.

Allowed verbs. Sole author. Built. Compiles. Designed recovery. Do not write architected.

Likely follow-ups. See `interview_defense.md`.

Acceptable wording. Local browser operator reads one compiled runtime. Submit is allowed only after listed identity and writing checks. Tests refuse passwords, cookies, OTP, and leaked phone or email.

### mixhvg_py

Role. Library API and correctness.

Unique signal. AnnData interface and a fidelity harness.

Page space. Cells-by-genes API. Maximum-rank ensemble. Spearman 1.0000 and Jaccard 0.980. Seurat 5 silent-NULL pinned in tests. 68 pytest tests. R / scran / Seurat version triples stay in the bank. The number gate does not load `research_evidence`.

Deliberately omitted. mv_ct and mv_nc mismatch. Bit-exact scran_pos. Production users outside speciesOT. Weighted-median scran correction. That numerical story belongs on Health AI. Zhao et al. method credit stays off this title and lives in defense notes.

Allowed verbs. Sole author. Ported. Validated. Reproduced. Do not write designed the method.

### compleg_uk_nz

Role. Data-pipeline research that still reads as software.

Unique signal. ETL, schema, anti-bot recovery, AWS staging. No other frozen family carries this project.

Page space. 22,606 votes, 4,791 MPs, 4,108 bills, 3,875 constituencies. Cloudflare 403. Selenium. AWS EC2 and S3. Handoff note.

Deliberately omitted. 57,000+ across seven tables. 59 commits. 58 MB repo. Invented counts. The bank recovered these from committed CSVs after the user asked for invented numbers and was declined.

Allowed verbs. Research Assistant. Built. Diagnosed. Restored. Staged. Wrote.

## Skills line

Technologies the selected projects use. Python. SQL. YAML policy compile. Selenium. schemas. unittest. pytest. AWS EC2. S3. boto3. ETL. AnnData.

Drop. Kubernetes. JavaScript and React. Clinical stack. Polar runtime compiler. G2 and G3.

AWS certifications stay as profile facts already on the master.

## Catalog

Do not use a stronger catalog paraphrase. Bank sentences only.
