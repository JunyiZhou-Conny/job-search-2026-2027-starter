# Semantic review. `swe_v1`

Heuristic `arena()` was not used as recruiter judgment.

Two structurally different candidates were written and fact-checked before this review.

## Candidates

**A. Ownership, library, pipeline.** Job Search OS, mixhvg-py, Compleg. Engineering then Research.

**B. Automation pair plus pipeline.** Job Search OS, AutoResearch, Compleg. Replaces the library with the Cannon loop.

Both candidates used only bank-supported numbers and verbs. Neither entered this review with a factual veto.

Opponent. `tests/fixtures/resume_quality/baselines/2026-08-24_cloud-swe_v1.3.tex`. That page leads with a clinical chatbot and titles AutoResearch an agent.

## Evidence auditor

Candidate A and candidate B both stay inside usable bank claims. No Kubernetes. No planned GRPO as done. Compleg counts match committed CSV figures in the bank.

`python3 scripts/resume_quality.py build --family swe --compile` is the production gate for the winner.

## Technical recruiter

Can a recruiter see a software engineer in 10-15 seconds?

Candidate A. Yes. First heading is an owned system with compile and tests. Second is a public API with a test harness. Research is ETL, schema, and AWS. Skills look like software.

Candidate B. Also yes, but the second heading is a cluster experiment loop. That scan collides with `ai_infra_v1`.

Preference. A.

## Senior engineer

Do I see implementation and operational judgment?

Candidate A. Policy compile, secret-refusing tests, recovery, AnnData API, pinned upstream defects, Cloudflare 403, EC2 and S3. Those are software jobs.

Candidate B. Adds a real checkpointed loop. Loses the only public library API. Two automation stories spend the same signal twice.

Preference. A.

## Skeptical technical interviewer

Are the verbs defensible?

Job Search OS "Designed recovery" matches `strongest_accomplishments`. Tests that refuse leaked secrets match `software_engineering_evidence`. Compleg "relational schema designed up front" is in the bank. mixhvg does not claim Zhao's method.

Candidate B's AutoResearch 338 / 79.3 h line is `resume_ok: true` and still the first hard provenance question. It is already on `ai_infra_v1`. Putting it here invites the same interview twice and weakens family identity.

Preference. A.

## Evidence auditor on family identity

If SWE and AI Infra share Job Search OS plus AutoResearch, a recruiter cannot tell why two PDFs exist. Compleg and mixhvg are the SWE-only payments. Keep them.

## Disagreements

The senior engineer liked AutoResearch's checkpoint story more than mixhvg's biology-adjacent API. The recruiter and the interviewer did not. Graft decision. Keep mixhvg. Do not import AutoResearch bullets into JSOS.

No hybrid project slate. Winner is A as written.

## Pairwise question

Would a reasonable SWE recruiter learn a clearer truthful software story from this page than from the 2026-08-24 cloud-swe cluster or from `ai_infra_v1`?

Yes. The old cluster spends an engineering slot on a clinical chatbot. AI Infra is experiment loops and agent gates. This page is owned software, a tested API, and a data pipeline.
