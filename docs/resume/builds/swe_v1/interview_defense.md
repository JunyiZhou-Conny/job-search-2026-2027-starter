# Interview defense. `swe_v1`

These boundaries stay here and in the evidence bank.

## job_search_os

Expect. Does this system apply for you by itself?
Answer. No. Polar Local may Submit after listed identity and writing gates. Cloud G2/G3 stay closed. The system does not send messages or apply on its own.

Expect. Why is the title not Job Search OS?
Answer. Recruiter-facing title leads with the capability. The first bullet names job applications. The same repo appears on `ai_infra_v1` with agent-systems framing. This page pays compile, tests, and recovery.

Expect. How many applications did it submit?
Answer. Do not answer with `data/applications.csv` applied counts. Those are ledger statuses, `resume_ok: false`.

Expect. What do the tests actually refuse?
Answer. Passwords, cookies, OTP, leaked phone or email, missing policy sections, and contradictory identity facts. Live unittest totals stay off the resume because they rot.

## mixhvg_py

Expect. Did you invent mixhvg?
Answer. No. Zhao et al. 2024. This is a validated Python port with an AnnData API.

Expect. Is every method bit-exact?
Answer. seuratv3 / seuratv1 match at reported precision. scran_pos is seed-dependent. mv_ct and mv_nc do not match and are excluded.

Expect. Why is this on a SWE page?
Answer. The paid signal is the API and the fidelity harness, not the biology. The Health AI page uses the same repo for the HVG method.

Expect. Is it used in production?
Answer. speciesOT specs set `hvg_method: mixhvg`. Do not claim other production users.

## compleg_uk_nz

Expect. Where do the vote counts come from?
Answer. Committed CSVs counted on 2026-07-29. The user did not remember the figures and asked for invented numbers. Those were declined.

Expect. Did you design the legislative research question?
Answer. Research Assistant for Carrubba and Estrada. The software work is scrapers, schema, anti-bot recovery, AWS staging, and a handoff note.

Expect. 57,000+ records?
Answer. That is a bank total across seven measured tables. It is not on the page. Stick to the four headline counts.
