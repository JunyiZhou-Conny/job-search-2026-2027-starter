# /tailor-resume

1. Load the approved base resume from `resumes/base/JZ_resume.tex` plus the evidence bank.
2. Propose ≤2–4 bullet edits with requirement → evidence → wording.
3. Do not invent skills. Preserve base density/specificity.
4. After edits, run `./scripts/compile_resume.sh` and record a new version hypothesis.
