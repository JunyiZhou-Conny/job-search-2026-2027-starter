# /tailor-resume

1. Load nearest cluster from `data/resume_versions.csv` + evidence bank.
2. Propose ≤2–4 bullet edits with requirement → evidence → wording.
3. Do not invent skills. Preserve base density/specificity.
4. After edits, run `./scripts/compile_resume.sh <cluster>` and record new version hypothesis.
