# /generate-calendar

1. Run `python3 scripts/generate_calendar.py` (dry-run) and show planned events.
2. Ask user before `python3 scripts/generate_calendar.py --write`.
3. Do not touch Google Calendar unless explicitly requested and configured.
