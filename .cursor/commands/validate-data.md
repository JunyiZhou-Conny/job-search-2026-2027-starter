# /validate-data

1. Run `python3 scripts/validate_data.py`.
2. Run `python3 scripts/dedupe_applications.py`.
3. Summarize `generated/review_queue.csv` and `generated/review_queue_dedupe.csv`.
4. Propose fixes; do not delete duplicate rows automatically.
